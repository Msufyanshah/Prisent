# TASKS — Prisent — WEEK 3

> Version: 3.1 | Status: Active
> Week 3 Theme: Polish, Deploy & Launch.
> Continues numbering from Week 2 (TASK-036 onward).

---

## Week 3 Exit Criteria
Before launch, all of the following must be true:
- [ ] Product live on public URL (FastAPI on Railway, Next.js on Vercel)
- [ ] Real Analytics dashboard fetching live sync statistics from backend
- [ ] AI Insight Card active, presenting actionable marketing insights
- [ ] Secure JWT authentication and token encryption active on all routes
- [ ] 2 real LinkedIn posts successfully published via the tool
- [ ] Environment variables and secrets confirmed safe for public deployment
- [ ] LinkedIn mock mode disabled/guarded in any non-development environment
- [ ] Sidebar collapse preference (TASK-029b) implemented, if scoped for launch
- [ ] Prisent "P" logo asset finalized as production SVG and in use across the app
- [ ] No known data-integrity issues (e.g. malformed IDs) in production tables

---

### TASK-036 — Analytics Dashboard API Integration

**Day:** D11 | **Estimate:** 2h | **Depends on:** TASK-035 | **Status:** done

**WHAT:** Connect the frontend `/dashboard/analytics` page to the backend
`/analytics/summary` and `/analytics/posts` endpoints.

**WHY:** Replaces static mockup metrics with real data fetched dynamically
from the database.

**FILES TO MODIFY:**
- `frontend/app/dashboard/analytics/page.tsx`

**VERIFICATION (before trusting "done"):**
- [ ] Hit `/analytics/summary` and `/analytics/posts` directly (curl/browser)
      and confirm real, non-mock data is returned
- [ ] Confirm the frontend renders that data, not hardcoded placeholder values
- [ ] Confirm behavior when a user has zero posts (empty state, not a crash)

---

### TASK-032b — LinkedIn Integration UI on Settings Page

**Day:** D11 | **Estimate:** 2h | **Depends on:** TASK-021 | **Status:** done

**WHAT:** Add LinkedIn connection UI to settings page (connect button, status
display, and disconnect button) using status and disconnect routes.

**WHY:** Provides a way for the user to initiate and manage the OAuth flow
from the frontend settings.

**FILES TO MODIFY:**
- `backend/app/routes/linkedin_auth.py`
- `frontend/lib/api.ts`
- `frontend/lib/types.ts`
- `frontend/app/dashboard/settings/page.tsx`

**VERIFICATION (before trusting "done"):**
- [ ] Screenshot the actual Settings page showing connect/disconnect states
- [ ] Confirm disconnect actually clears `linkedin_access_token` /
      `linkedin_person_id` in the database, not just the UI state

---

### TASK-037 — Fix `linkedin_person_id` Data Integrity (Mock Mode Cleanup)

**Day:** D11 | **Estimate:** 1h | **Depends on:** TASK-032b | **Status:** todo

**WHAT:** Investigate and fix the root cause of pipe-delimited values
(e.g. `"mock_person_id_999|LinkedIn Mock User"`) appearing in the
`linkedin_person_id` column, rather than patching around it at publish time.

**WHY:** The publishing fix referenced in agent status updates extracts the
ID portion before a `|` character at publish time — this is a symptom-level
patch. The real question is why a combined `id|name` string was ever written
to a column that should only ever hold the LinkedIn `sub` value. If this
originated from mock-mode data (`get_linkedin_profile`'s mock return in
`linkedin_oauth.py`), any real user record containing this pattern needs to
be identified and cleaned before launch.

**HOW:**
1. Audit `get_linkedin_profile()` and the callback handler in
   `linkedin_auth.py` — confirm exactly which field, real or mock, could
   produce a `id|name` combined string
2. Fix the write path so `linkedin_person_id` only ever stores the bare
   `sub` value, never a combined string
3. Remove the publish-time `.split("|")` workaround once the write path is
   fixed at the source
4. Run a one-time query against the `users` table to check for any existing
   rows with a `|` character in `linkedin_person_id`, and clean them

**ACCEPTANCE CRITERIA:**
- [ ] Root cause identified and documented (not just patched)
- [ ] `linkedin_person_id` write path only ever stores a bare ID
- [ ] Publish-time string-splitting workaround removed
- [ ] Existing malformed rows (if any) cleaned in the database

---

### TASK-038 — Guard Mock Mode Against Non-Development Use

**Day:** D11 | **Estimate:** 1h | **Depends on:** TASK-021 | **Status:** todo

**WHAT:** Ensure the `LINKEDIN_CLIENT_ID == "mock-id"` sandbox path in
`linkedin_oauth.py` cannot be reached in staging or production.

**WHY:** Mock mode currently short-circuits real LinkedIn OAuth whenever the
env var equals `"mock-id"`. This was essential for local development while
waiting on LinkedIn product approval, but if this code path is ever
reachable in a deployed environment, it would let anyone fake a LinkedIn
connection without ever touching LinkedIn's real API — a real security and
data-integrity risk once the product is public.

**HOW:**
```python
# In each of the three mock-mode checks in linkedin_oauth.py, add an
# explicit environment guard:
if settings.LINKEDIN_CLIENT_ID == "mock-id":
    if settings.ENV != "development":
        raise RuntimeError(
            "Mock LinkedIn mode is not permitted outside development"
        )
    return ...  # existing mock behavior
```

**ACCEPTANCE CRITERIA:**
- [ ] Mock mode raises an explicit error if `ENV` is not `"development"`
- [ ] Confirm `ENV` is set correctly in both Railway (backend) and Vercel
      (frontend, if relevant) deployment configs
- [ ] Manually test: attempting mock mode with `ENV=production` locally
      fails loudly, not silently

---

### TASK-039 — Backend Deployment (Railway)

**Day:** D12 | **Estimate:** 2h | **Depends on:** TASK-038 | **Status:** todo

**WHAT:** Deploy the FastAPI backend to Railway with production environment
variables configured.

**WHY:** Required for the app to be reachable on a public URL, per the
Week 3 exit criteria.

**HOW:**
1. Create a Railway project, connect the backend repo/branch
2. Configure production environment variables: `DATABASE_URL`,
   `LINKEDIN_CLIENT_ID`, `LINKEDIN_CLIENT_SECRET`,
   `LINKEDIN_REDIRECT_URI` (production callback URL), `JWT_SECRET`,
   `QDRANT_URL` (or hosted Qdrant instance, not `:memory:` — see TASK-041),
   `ENV=production`
3. Add the production callback URL
   (`https://<your-backend-domain>/auth/linkedin/callback`) to the
   LinkedIn Developer app's Authorized redirect URLs, alongside the
   existing localhost one
4. Run production database migrations
5. Confirm CORS middleware allows the production frontend origin

**ACCEPTANCE CRITERIA:**
- [ ] Backend reachable on a public Railway URL
- [ ] `/health` (or equivalent) endpoint returns 200
- [ ] Production LinkedIn redirect URI registered and working
- [ ] No `.env` secrets committed to the repo

---

### TASK-040 — Frontend Deployment (Vercel)

**Day:** D12 | **Estimate:** 1.5h | **Depends on:** TASK-039 | **Status:** todo

**WHAT:** Deploy the Next.js frontend to Vercel, pointed at the production
backend URL.

**WHY:** Required for a public, usable product per the exit criteria.

**HOW:**
1. Connect the frontend repo to Vercel
2. Set `NEXT_PUBLIC_API_URL` (or equivalent) to the Railway backend's
   public URL
3. Confirm build succeeds and environment variables are set per-environment
   (not just locally)

**ACCEPTANCE CRITERIA:**
- [ ] Frontend live on a public Vercel URL
- [ ] Login, dashboard, and all 4 core pages load correctly against the
      production backend
- [ ] LinkedIn connect flow works end-to-end in production (real OAuth,
      not mock mode — confirms TASK-038's guard is working)

---

### TASK-041 — Replace In-Memory Qdrant with a Persistent Instance

**Day:** D12 | **Estimate:** 1.5h | **Depends on:** TASK-039 | **Status:** todo

**WHAT:** Move off `QDRANT_URL=":memory:"` for any deployed environment —
use a hosted or persistent Qdrant instance instead.

**WHY:** In-memory mode was fine for local development, but it means voice
memory / semantic retrieval data is wiped on every backend restart. In
production, a Railway redeploy or crash would silently erase all indexed
voice memory. This directly undermines TASK-037's earlier fix (which made
the in-memory client a shared singleton) — that fix solved the crash, but
persistence across restarts is a separate, still-open problem.

**HOW:**
1. Provision a persistent Qdrant instance (Qdrant Cloud free tier, or a
   Railway-hosted Qdrant service)
2. Set `QDRANT_URL` in production to the real instance URL, not `:memory:`
3. Confirm existing seed/index logic still runs correctly against a real
   instance (not just the in-memory shortcut path)

**ACCEPTANCE CRITERIA:**
- [ ] Production `QDRANT_URL` points to a persistent instance
- [ ] Voice memory data survives a backend restart/redeploy in production
- [ ] Local development can still use `:memory:` for speed — this task
      only changes production/staging behavior

---

### TASK-042 — Re-enable Full LinkedIn Scopes & Publish 2 Real Posts

**Day:** D13 | **Estimate:** 1h | **Depends on:** TASK-040 | **Status:** todo

**WHAT:** Confirm `SCOPES` in `linkedin_oauth.py` includes `w_member_social`
(now that Share on LinkedIn is approved), and publish 2 real posts to
LinkedIn from the deployed production app.

**WHY:** This is the literal exit criterion — "2 real LinkedIn posts
successfully published via the tool" — and needs to happen against the
production deployment, not just localhost, to count as proof the whole
pipeline works publicly.

**HOW:**
1. Confirm `SCOPES = ["openid", "profile", "w_member_social"]` in the
   deployed backend
2. From the production frontend, connect a real LinkedIn account (full
   consent screen, not mock mode)
3. Generate and publish one post via the normal flow
4. Generate and publish a second post, ideally on a different content
   pillar, to confirm the flow isn't a one-off fluke

**ACCEPTANCE CRITERIA:**
- [ ] 2 posts visible on the real LinkedIn account, published via Prisent
- [ ] Both posts show correct author attribution (confirms TASK-037's URN
      fix holds under real, non-mock data)
- [ ] Impressions/engagement data for both posts flows into
      `/analytics/summary` correctly

---

### TASK-043 — Verify Generated Content Grounding (No Fabricated Claims)

**Day:** D13 | **Estimate:** 1.5h | **Depends on:** TASK-042 | **Status:** todo

**WHAT:** Review the writer/reviewer agent prompts and confirm generated
posts do not invent specific, unverifiable factual claims (statistics,
named projects, quantified outcomes) that the user never provided.

**WHY:** A real published post included a specific, quantified claim
("slashed error rates by 25%") that was not sourced from any persona field
the user entered. Publishing fabricated professional claims under a real
person's LinkedIn identity is a credibility and trust risk, not just a
style issue — this needs an explicit guardrail before broader use.

**HOW:**
1. Review the writer agent's system prompt — check whether it's
   instructed to draw only from the user's actual `content_pillars`,
   `unique_differentiator`, and `target_audience` fields, or whether it's
   free to generate illustrative examples/statistics on its own
2. Add an explicit constraint: generated posts may make general
   observations and arguments, but must not state specific numbers,
   named outcomes, or "our team did X" claims unless those specifics
   were present in the user's own input data
3. Have the reviewer agent specifically flag (not just score) any
   fabricated-sounding specific claim before a post reaches "draft" state

**ACCEPTANCE CRITERIA:**
- [ ] Writer agent prompt updated with an explicit no-fabrication
      constraint
- [ ] Reviewer agent checks for and flags unverified specific claims
- [ ] Manually generate 5 test posts and confirm none introduce numbers,
      named projects, or outcomes not present in the source persona data

---

### TASK-044 — Sidebar Collapse Persistence (Backend + Frontend)

**Day:** D13 | **Estimate:** 2h | **Depends on:** TASK-029 | **Status:** todo
**Note:** Corresponds to the previously-scoped TASK-029b. Include here only
if collapse/expand is going into this launch; otherwise move to a post-launch
backlog and remove from Week 3 exit criteria.

**WHAT:** Add `sidebar_collapsed: boolean` to the User model, a
`PATCH /api/user/preferences` endpoint, and wire the frontend toggle to
persist and restore this state.

**WHY:** Previously scoped and agreed on, but not yet confirmed built.
Decide explicitly whether this ships at launch or is deferred — don't let
it sit ambiguously "maybe done."

**ACCEPTANCE CRITERIA:**
- [ ] `sidebar_collapsed` field exists and persists via the API
- [ ] Sidebar restores the correct state on reload/re-login
- [ ] If deferred instead: removed from the Week 3 exit criteria list
      above and logged as a known post-launch item

---

### TASK-045 — Finalize Production Logo SVG

**Day:** D13 | **Estimate:** 1h | **Depends on:** none | **Status:** todo

**WHAT:** Replace any hand-approximated or placeholder logo path with a
properly vector-traced, spec-accurate SVG of the Prisent "P" mark.

**WHY:** Flagged earlier as needing a real trace against the original
reference image and milestone2's construction spec — confirm this was
completed with a real vector source before shipping it across every screen.

**ACCEPTANCE CRITERIA:**
- [ ] Confirm the SVG currently in use was properly traced, not
      hand-guessed
- [ ] Both light (`#FAFAFA`) and dark (`#09090B`) flat variants exist
- [ ] Renders cleanly at 16px, 24px, and larger sizes with no distortion

---

### TASK-046 — Pre-Launch Secrets & Security Audit

**Day:** D13 | **Estimate:** 1h | **Depends on:** TASK-039, TASK-040 | **Status:** todo

**WHAT:** Final check that no secrets are exposed and all routes are
properly authenticated before calling this launched.

**HOW:**
1. Confirm `.env` is in `.gitignore` and no secret has ever been committed
   (check git history, not just the current working tree)
2. Confirm `LINKEDIN_CLIENT_SECRET` and `JWT_SECRET` are only present in
   Railway's environment config, never in frontend code or public repos
3. Confirm all dashboard API routes require authentication
   (`require_auth` or equivalent) and reject unauthenticated requests
4. Confirm TASK-038's mock-mode guard is active and verified in production

**ACCEPTANCE CRITERIA:**
- [ ] No secrets in git history or frontend bundle
- [ ] All protected routes return 401 without a valid token
- [ ] Mock mode confirmed unreachable in production
